{
  "compilerOptions": {
    "target": "es2018",                        /* Especifica la versión de ECMAScript de destino. */
    "module": "commonjs",                     /* Especifica el sistema de módulos para la salida. */
    "lib": ["es2018"],                        /* Lista de bibliotecas que se incluirán en el entorno de compilación. */
    "outDir": "./dist",                       /* Directorio de salida para los archivos JavaScript compilados. */
    "rootDir": "./src",                       /* Directorio raíz de donde se encuentran los archivos fuente TypeScript. */
    "strict": true,                           /* Habilita todas las opciones de verificación de tipo estricta. */
    "esModuleInterop": true,                  /* Permite la interoperabilidad con módulos CommonJS para importaciones de estilo ES6. */
    "skipLibCheck": true,                     /* Omite la verificación de tipos de los archivos de definición. */
    "forceConsistentCasingInFileNames": true, /* Garantiza que las referencias a los archivos utilicen mayúsculas y minúsculas consistentes. */
    "resolveJsonModule": true                 /* Permite importar módulos con una extensión `.json` */
  },
  "include": ["src/**/*.ts"],                 /* Incluye todos los archivos .ts dentro de la carpeta src. */
  "exclude": ["node_modules"]                 /* Excluye el directorio node_modules. */
}